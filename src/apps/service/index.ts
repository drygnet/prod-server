import { templateItem } from './schemas';

const templateApp = {
    collections: [
        {
            name: 'cases',
            schema: templateItem
        }
    ]
};

export default templateApp;
