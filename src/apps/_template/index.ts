import { templateItem } from './schemas';

const templateApp = {
    collections: [
        {
            name: 'templateCollection',
            schema: templateItem
        }
    ]
};

export default templateApp;
